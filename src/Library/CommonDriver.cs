namespace PII_Herencia
{
    public class CommonDriver:Driver
    {
        public CommonDriver(string ID, string name, string surename, string vehicle, string bio): base (ID, name, surename, vehicle,bio)
        {
        }
        
    }
}